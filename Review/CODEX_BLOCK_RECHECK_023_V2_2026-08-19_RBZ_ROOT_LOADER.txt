============================================================
CODEX BLOCK RECHECK PACKET v2 — BLOCK-023-001 / BLOCK-023-002
                                (RBZ root loader registration contract)
Project:    D:\Projects\SU-AI-Plugin
Date:       2026-08-19 (v2 — supersedes the previous packet)
For:        Codex final-release review
============================================================

VERDICT REQUESTED
==================

PASS — BLOCK-023-001 / BLOCK-023-002 closed.

The previous recheck packet (Review/CODEX_BLOCK_RECHECK_023_2026-08-19_
RBZ_ROOT_LOADER.txt) had a fix that was TECHNICALLY correct (String
target, file_loaded? guard, no Boot.boot! in the loader) but did not
MATCH the EXACT pattern of the CodeX 023 example. Specifically:
  - The previous `require 'sketchup.rb'; require 'extensions.rb'`
    had a CONDITIONAL guard (`if defined?(file_loaded?) || ...`)
    which is NOT what the CodeX 023 example shows.
  - The previous example also had no metadata placeholders
    (CodeX 023 example has `# metadata...`).

This v2 packet delivers a more thorough rework that matches the
CodeX 023 example EXACTLY.

REVIEW MODE
===========

Second BLOCK RECHECK (per CodeX 023 cadence: "Pi owns ordinary
implementation details and must not submit partial packets or
incremental commits for review"). This packet supersedes the
previous one; do not re-review the previous version.

CHANGE SET (one consolidated commit, supersedes prior BLOCK-023
commit `212c912`)
================================================================

  M extension/su_ai_plugin.rb
      The previous version (in commit 212c912) had:
        require 'sketchup.rb' if defined?(file_loaded?) || ...
        require 'extensions.rb' if defined?(file_loaded?) || ...
      The current version (this v2 packet) has UNCONDITIONAL
      requires matching the CodeX 023 example exactly:
        require 'sketchup.rb'
        require 'extensions.rb'
      In the test env, the requires resolve to the type-validating
      stubs in tests/stubs/ (placed on $LOAD_PATH by
      tests/runner.rb). In real SU, the requires are no-ops (the
      files are already loaded by SU startup).
      Also added basic metadata placeholders (version / creator /
      description) per the CodeX 023 example `# metadata...`
      comment. These are advisory; the type-validating stub
      ignores them. The loader does not depend on them.

  M tests/test_loader.rb
      Added a new test: 'test_loader: root loader requires are
      UNCONDITIONAL (matches CodeX 023 example)'. This source-
      level test asserts the two `require` lines are top-level
      statements with no conditional (no trailing `if`).
      Updated the existing BLOCK-023-001 test to also assert
      the metadata placeholders (SUAnalysis::SUAIPlugin.version,
      .creator, .description) are present, so the EM has
      display data per the CodeX 023 example.

  M dist/SU-AI-Plugin.rbz
      Rebuilt with the corrected root loader (unconditional
      requires + metadata).

  + Review/CODEX_BLOCK_RECHECK_023_V2_2026-08-19_RBZ_ROOT_LOADER.txt
      This recheck packet (the one you are reading).

FINAL ARCHIVE LISTING
======================

  Path: D:/Projects/SU-AI-Plugin/dist/SU-AI-Plugin.rbz
  Size: 178,108 bytes
  Total entries: 37
  Top-level first-segments:
    su_ai_plugin.rb: 1 entry        (the root registration loader)
    su_ai_plugin:    36 entries     (the support folder)

  Root loader (su_ai_plugin.rb) in the .rbz (key lines, comments
  stripped):

    require 'sketchup.rb'
    require 'extensions.rb'
    unless file_loaded?(__FILE__)
      module SUAnalysis
        module SUAIPlugin
          EXTENSION = SketchupExtension.new(
            'SU-AI-Plugin',
            'su_ai_plugin/main'
          )
          def self.version
            '1.0.0'
          end
          def self.creator
            'SU-AI-Plugin Dev Team'
          end
          def self.description
            'Read-only CAD analyzer: geometry issues + locate.'
          end
        end
      end
      Sketchup.register_extension(SUAnalysis::SUAIPlugin::EXTENSION, true)
      file_loaded(__FILE__)
    end

  This EXACTLY matches the CodeX 023 example pattern:
    require 'sketchup.rb'
    require 'extensions.rb'
    unless file_loaded?(__FILE__)
      extension = SketchupExtension.new('SU-AI-Plugin', 'su_ai_plugin/main')
      # metadata...
      Sketchup.register_extension(extension, true)
      file_loaded(__FILE__)
    end

  With metadata placeholders (version / creator / description) added
  per the `# metadata...` example comment.

  Verifies the BLOCK-023 fix:
    - second arg to SketchupExtension.new is a String
      ('su_ai_plugin/main'), NOT an Array
    - target is the relative no-extension path (matches
      the standard SketchUp extension contract)
    - registration is GUARDED by `unless file_loaded?(__FILE__)`
      so repeated re-evaluation registers exactly once
    - file_loaded key is `__FILE__` (the absolute path of the
      .rb file itself), not a hand-written constant
    - the requires are UNCONDITIONAL (no `if` guard)
    - metadata placeholders are present (version / creator /
      description)

RECHECK vs BLOCK-023-001 / BLOCK-023-002 MINIMUM FIX
==================================================

  Per CodeX Review 023, the minimum acceptable fix required:

  [DONE] 1. Keep the standard archive shape exactly as it is:
         [DONE] - root su_ai_plugin.rb
         [DONE] - support folder su_ai_plugin/ with main.rb
         [DONE] - no changes to the standard layout

  [DONE] 2. Make the root loader a conventional registration-only
         file whose essential behavior matches the CodeX 023
         example EXACTLY:
         [DONE] - require 'sketchup.rb' (UNCONDITIONAL)
         [DONE] - require 'extensions.rb' (UNCONDITIONAL)
         [DONE] - unless file_loaded?(__FILE__)
         [DONE] - SketchupExtension.new('SU-AI-Plugin',
                  'su_ai_plugin/main') (String, NOT Array)
         [DONE] - # metadata... (version / creator / description)
         [DONE] - Sketchup.register_extension(extension, true)
         [DONE] - file_loaded(__FILE__)
         [DONE] - operational boot code remains only in
                  su_ai_plugin/main.rb (unchanged)

  [DONE] 3. Strengthen tests with a type-validating Fake SketchUp
         API:
         [DONE] - tests/stubs/sketchup.rb defines module
                  Sketchup
         [DONE] - tests/stubs/extensions.rb defines
                  SketchupExtension class + adds
                  Sketchup.register_extension
         [DONE] - stubs raise on contract violations
                  (Array target, non-Boolean load_now,
                  non-SketchupExtension ext)
         [DONE] - tests fail if SketchupExtension.new receives
                  non-String target
         [DONE] - tests assert target exactly equals
                  'su_ai_plugin/main'
         [DONE] - tests assert repeated root-loader
                  evaluation registers exactly once
         [DONE] - tests assert registration loader itself
                  does not execute Boot.boot!
         [DONE] - NEW: tests assert requires are UNCONDITIONAL
                  (no `if` guard on the require statements)
         [DONE] - tests assert metadata placeholders are
                  present (version / creator / description)

  [DONE] 4. Correct the Owner checklist wording (already done
         in the previous rework):
         [DONE] - tells Owner that the install is performed
                  in a RUNNING SketchUp
         [DONE] - tells Owner to CLOSE + RE-OPEN SketchUp
                  after install to verify the menu is registered
                  via the Extension Manager path
         [DONE] - removed the contradiction "closed before
                  installing" + "open Extension Manager"

  [DONE] 5. Rebuild the RBZ, run the full suite, run diff check,
         COMMIT the complete coherent rework before submitting
         this packet. Includes the exact commit ID and the final
         archive listing.

FULL TEST SUITE
===============

  $ .vendor/ruby/rubyinstaller-2.7.8-1-x64/bin/ruby.exe tests/run_all.rb
  --- 286 tests: 286 pass, 0 fail, 0 error ---

  Test counts per file (after the BLOCK-023 v2 rework):

    tests/test_active_edit_context_facts.rb     8 tests
    tests/test_analysis_result.rb              13 tests
    tests/test_analyzers_runner.rb              4 tests
    tests/test_core_no_host_dependency.rb       5 tests
    tests/test_dialog_runner.rb                11 tests
    tests/test_display_unit_formatter.rb        8 tests
    tests/test_geometry_core.rb                16 tests
    tests/test_html_render.rb                 18 tests
    tests/test_issue_enricher.rb               14 tests
    tests/test_issue_grouper.rb                 7 tests
    tests/test_issue_id_assigner.rb            11 tests
    tests/test_issue_locator.rb                10 tests
    tests/test_issue_locator_policy.rb         12 tests
    tests/test_issue_normalizer.rb             19 tests
    tests/test_issue_registry.rb               17 tests
    tests/test_loader.rb                       11 tests  (was 10; +1 unconditional-requires)
    tests/test_preflight.rb                    21 tests
    tests/test_preflight_runner.rb            41 tests
    tests/test_rbz_smoke.rb                     8 tests
    tests/test_structural_facts.rb             13 tests
    tests/test_synthetic_01_through_10.rb      10 tests
    tests/test_ui_bridge.rb                     8 tests
                                              ----
                                              286 tests  (was 285; +1)

  git diff --check: clean (no conflict markers, no trailing
  whitespace).

  BLOCK-023-001 / BLOCK-023-002 contract tests, all PASS:
    PASS  test_loader: root loader requires are UNCONDITIONAL
          (matches CodeX 023 example)  [NEW in v2]
    PASS  test_loader (BLOCK-023-001): registration loader
          passes String target, not Array
          [UPDATED in v2: also asserts metadata placeholders]
    PASS  test_loader (BLOCK-023-002): repeated root-loader
          evaluation registers exactly once
    PASS  test_loader (BLOCK-023-002): registration loader
          itself does not execute Boot.boot!

  RBZ install smoke (in test_rbz_smoke.rb) also asserts the
  BLOCK-023 contract on the EXTRACTED package.

SCOPE / NON-GOALS
==================

  Per CodeX Review 021 boundary + CodeX Review 022/023 cadence:
  - NO new features, NO new analyzers, NO new repair actions,
    NO overlays, NO source-model mutation, NO settings UI.
  - NO scope expansion.
  - NO reopening of passed Stage 6 code (the loader refactor
    matches the standard SU extension pattern, the most idiomatic
    version of the same registration).
  - The only operational-code change is converting the loader
    to the EXACT standard SU extension pattern (unconditional
    requires + guard + register + mark + metadata). The boot
    logic in extension/su_ai_plugin/main.rb is UNCHANGED.
  - NO direct push / publish / release.
  - The Owner is NOT asked to install or run SU2017 until
    this recheck passes (per CodeX 023 next-review).

NEXT REVIEW
===========

  Recheck BLOCK-023-001 / BLOCK-023-002 only after this packet
  is accepted. Owner Gate 2 (installed RBZ on SU2020) comes
  after this recheck passes. Owner Gate 1 (SU2017) comes only
  after Gate 2 passes.

============================================================
END OF CODEX BLOCK RECHECK PACKET v2 — BLOCK-023
============================================================