============================================================
OWNER VERIFICATION CHECKLIST — RBZ Install Smoke (release gate 2)
Project:    D:\Projects\SU-AI-Plugin
Date:       2026-08-19 (revised v2 per CodeX Review 022 BLOCK-022-001)
For:        Product Owner (real-SU install verification, per Q002=A)
Depends on: CodeX Review 022 release-package contract
============================================================

PURPOSE
=======

Per CodeX Review 021 (2026-08-19) Stage 7 implementation report, two
release gates remain open after TASK 001 implementation:

  Gate 1 — SU2017 real-host verification (per R004; Owner executes
           on a SU2017 host; SU2020 evidence does not substitute).

  Gate 2 — RBZ package + install smoke verification (this checklist;
           Agent built the candidate .rbz + fake-host smoke; Owner
           verifies a real SketchUp install of the same .rbz).

This file is the manual checklist for Gate 2 only. The Agent's
fake-host evidence (tests/test_rbz_smoke.rb, 8/8 PASS) is documented
in Review/RBZ_RELEASE_CANDIDATE_2026-08-19.txt and committed
alongside this file.

Per CodeX Review 022 (2026-08-19) BLOCK-022-001: the .rbz has a
STANDARD SketchUp extension layout (one root `.rb` + one same-named
support folder). The Owner MUST verify this layout on the installed
build before running the analyze flow.

RBZ ARTIFACT
============

  Path:     D:/Projects/SU-AI-Plugin/dist/SU-AI-Plugin.rbz
  Size:     ~178 KB (varies slightly as production code changes)
  Format:   Standard PKZip (uncompressed / STORE method); readable
            by 7-Zip / Windows Explorer / SketchUp Extension Manager.
  Layout:   Standard SketchUp extension contract:
              su_ai_plugin.rb                 # ROOT registration loader
              su_ai_plugin/                   # same-named support folder
                main.rb                       # boot target
                loader.rb
                core/...
                compatibility/...
                html/...

REQUIREMENTS
============

  - SketchUp 2017+ (locked baseline per Q003=R004). SU2020 is the
    verified target for Stage 6 Owner PASS; SU2017 is the
    separate release gate.
  - SketchUp must be CLOSED before installing (the Extension
    Manager requires it).
  - A disposable test model (NOT a production project). The
    checklist explicitly creates a test_group + uses a duplicate
    Component fixture; do NOT run this on real work.

PRE-INSTALL SMOKE (DO BEFORE INSTALLING)
========================================

Run the Agent-side fake-host suite to confirm the build artifact
is intact:

  cd D:\Projects\SU-AI-Plugin
  .vendor\ruby\rubyinstaller-2.7.8-1-x64\bin\ruby.exe tests\run_all.rb

Expected: 282 tests: 282 pass, 0 fail, 0 error (8 RBZ smoke
tests + 274 prior tests).

If the suite reports any failure, STOP — the .rbz is stale.
Rebuild with:

  .vendor\ruby\rubyinstaller-2.7.8-1-x64\bin\ruby.exe scripts\build_rbz.rb

INSTALL PROCEDURE
==================

  1. Open SketchUp 2020 (or 2017 if verifying Gate 1). The
     Extension Manager install is performed in a RUNNING SketchUp;
     the install is a one-time copy of the .rbz contents into
     SketchUp's Plugins/ directory. SketchUp does NOT load
     extensions at install time — it loads them at the NEXT
     SketchUp startup.

  2. Window > Extension Manager (SketchUp 2017+) OR
     Window > Preferences > Extensions (older builds).
     Confirm SketchUp is NOT loaded with any prior SU-AI-Plugin
     state. If a previous build is present, click "Uninstall"
     on it first.

  3. Click "Install Extension...". Navigate to:
        D:\Projects\SU-AI-Plugin\dist\SU-AI-Plugin.rbz

  4. SketchUp may show a security prompt ("You are about to
     load an extension that is not signed..."). Click "Yes" /
     "Load Anyway" (the .rbz is unsigned; this is the locked
     posture for V1.0 — no .rbz signing infrastructure yet).

  5. After install, SketchUp reports the extension is enabled.
     **CRITICAL**: SketchUp does NOT load the extension at install
     time. You MUST CLOSE and RE-OPEN SketchUp for the extension
     to load. After SketchUp restart, confirm the menu item
     appears at the EXPECTED LOCATION per the standard SketchUp
     extension contract:
        Plugins > SU-AI-Plugin > Analyze selection

     If the menu does NOT appear after the RESTART, the .rbz
     packaging is broken. STOP and report. Do NOT bypass the
     install by loading `extension/su_ai_plugin.rb` from the dev
     tree — that tests the dev tree and DOES NOT verify the
     install.

  6. After install, confirm the file layout under SketchUp's
     Plugins/ directory.
     The .rbz extracts into:
        SketchUp/Plugins/su_ai_plugin.rb          (root registration loader)
        SketchUp/Plugins/su_ai_plugin/            (same-named support folder)
          SketchUp/Plugins/su_ai_plugin/main.rb   (boot target)
          SketchUp/Plugins/su_ai_plugin/loader.rb
          SketchUp/Plugins/su_ai_plugin/core/...
          SketchUp/Plugins/su_ai_plugin/compatibility/...
          SketchUp/Plugins/su_ai_plugin/html/index.html
          SketchUp/Plugins/su_ai_plugin/html/app.js
          SketchUp/Plugins/su_ai_plugin/html/style.css

     The entry-point (`su_ai_plugin.rb`) is a SIBLING of the
     support folder (`su_ai_plugin/`), NOT a child. The support
     folder contains all operational code (main.rb, loader.rb,
     core/, compatibility/, html/).

POST-INSTALL BOOT SMOKE
=========================

  1. CLOSE SketchUp and RE-OPEN it. SketchUp loads extensions
     at startup; the install was a one-time copy. To verify
     the extension is registered, restart SketchUp.

  2. Open the Ruby Console (Window > Ruby Console).

  3. Confirm the entry-point loaded without error. Look for any
     [SU-AI-Plugin] boot failed: ... lines. Expected: none.

  4. Confirm the menu item appears at:
        Plugins > SU-AI-Plugin > Analyze selection

     If the menu is registered, the install is functioning.

  5. (Optional, advanced) Confirm the constants surface from
     the Ruby Console:
        SUAnalysis::Extension::Loader.registered?
        SUAnalysis::Core::PreflightReport.instance_methods(false)
     Expected: no NameError on the constants above; the Loader
     is loaded; PreflightReport exposes pure-Ruby metrics only.

ANALYZE SELECTION FLOW (REPEAT THE STAGE 6 K..N ON THE INSTALLED BUILD)
===========================================================================

Use the SAME setup helper from Review/OWNER_VERIFICATION_STAGE_6.txt
(rebuild a fresh empty model + the 4 fixtures). The installed
.rbz must produce the SAME results as Stage 6 K1..N (Owner PASS):

  K1 PASS: closed Group (4 edges) ->
          Edges: 4, Vertices: 4, Warnings: 0
  K2 PASS: two coincident Component instances ->
          Edges: 2, Duplicate Candidates: 1, Warnings: 0
  K3 PASS: nested Group/Component/Group (L-shape) ->
          Open Endpoints: 2, Deep Nesting: 1
  K4 PASS: dangling edge Group ->
          Open Endpoints: 2

If ANY of K1..N fails on the installed build but passed in Stage 6
on the dev-tree entry-point, the .rbz packaging is broken. STOP
and report the failing step with the Ruby Console log.

EXPECTED LOADER BEHAVIOR (post-install)
=========================================

  - After SketchUp restart, the Plugins > SU-AI-Plugin >
    Analyze selection menu is registered (one entry).
  - Clicking the menu opens the HtmlDialog.
  - The fingerprint helper from Stage 6 K..N matches before and
    after (no source-model mutation).
  - The dialog uses textContent only (no innerHTML for user
    strings), as enforced by tests/test_html_render.rb.

UNINSTALL (CLEANUP)
====================

After verification, uninstall to leave a clean state:

  1. SketchUp > Extension Manager > SU-AI-Plugin > Uninstall.
  2. Verify SketchUp/Plugins/su_ai_plugin.rb and
     SketchUp/Plugins/su_ai_plugin/ are both gone (SketchUp
     does NOT auto-delete the plugin's root .rb + folder on
     uninstall in some versions; manually remove if they
     remain).
  3. Re-open the Plugins menu; 'SU-AI-Plugin' submenu is gone.

REPORTING BACK
==============

After completing this checklist on real SketchUp, report one of:

  PASS ON SKETCHUP <version>
  - Install succeeded (no error from Extension Manager).
  - Menu registered exactly once after restart.
  - K1..N on the installed .rbz match Stage 6 K..N.
  - No Ruby Console errors during boot or analyze.
  - Uninstall is clean.

  PARTIAL PASS
  - List which sub-steps passed and which failed.
  - If a model-mutation is suspected, copy the offending
    fingerprint field and the failing step's Ruby Console log.

  BLOCKED
  - If the .rbz does not install at all, copy the SketchUp
    error verbatim and the .rbz path you tried.
  - If the menu does NOT appear after install, the .rbz
    packaging is broken. Do NOT bypass the install by
    loading the dev tree entry-point.

NEXT ACTION (AFTER GATE 2)
==========================

- If Gate 2 PASS on SU-2020: gate SU-2017 verification (Gate 1
  per R004). Repeat this checklist on a SU-2017 host.
- If Gate 1 PASS on SU-2017: TASK 001 is fully shipped (V1.0).
- If either gate fails on the real host, the Agent cannot proceed
  past the BLOCK until the failure is investigated (CodeX Round
  023 final release review cannot be requested).

============================================================
END OF OWNER VERIFICATION CHECKLIST — RBZ INSTALL (v2)
============================================================

============================================================
GATE 2 RESULT — SU2020 INSTALLED RBZ (verbally confirmed PASS)
============================================================

Status:                  PASS ON SKETCHUP 2020
Date (UTC):             2026-08-19
Reported by:            Owner (verbal confirmation, not via
                         this checklist; evidence retained by
                         Owner per the standalone Owner record).

Reported checklist items (per Checklist PASS criteria, line by
line):

  - Install succeeded (no error from Extension Manager):         PASS
  - Menu registered exactly once after restart:                 PASS
  - K1..N on the installed .rbz match Stage 6 K..N:           PASS
  - No Ruby Console errors during boot or analyze:              PASS
  - Uninstall is clean:                                         PASS

Specifically reported (per Owner):

  - After SketchUp restart, the Plugins menu contains
    `SU-AI-Plugin > Analyze selection` (one entry).
  - 4-edge closed Group smoke case produced:
        Edges: 4
        Vertices: 4
        Warnings: 0
  - K2 (two coincident Component instances): Edges: 2,
    Duplicate Candidates: 1, Warnings: 0.
  - K3 (nested Group/Component/Group, L-shape): Open Endpoints: 2,
    Deep Nesting: 1.
  - K4 (dangling edge Group): Open Endpoints: 2.
  - L (Locate): duplicate and nested Locate target the correct
    root occurrences; active_path unchanged; Deep Nesting is
    visibly non-actionable (no Locate, no toast).
  - M (stale source): in-dialog message + Ruby Console line; no
    crash or model change from the Locate click.
  - N (N MODEL UNCHANGED): MATCH = true on the closed Group.
  - Deep Nesting warning row: no `toast`, no `register_extension`
    call, no source mutation.

Reference: CodeX Review 024 (2026-08-19) PASS WITH NITS.
Two non-blocking nits from CodeX 024 (NOT reworked in this
release, deferred to post-release housekeeping per CodeX 024
explicit instruction):
  1. version/creator/description are module methods, not
     `SketchupExtension` object metadata setters.
  2. Constant-redefinition warnings in the test process from
     loading dev-tree + extracted-package copies together.

NEXT (per CodeX 024 explicit instruction):
  - Do NOT submit a new CodeX review packet between Gate 2 and
    Gate 1 unless a real runtime failure, data-safety issue, or
    unresolved high-risk ambiguity occurs.
  - Run the already-defined SU2017 real-host Gate 1 checklist
    (Review/OWNER_VERIFICATION_RBZ_INSTALL_2026-08-19.txt
    sections, repeated on a SU2017 host).
  - After BOTH gates PASS, the Agent assembles one consolidated
    final-release evidence packet for Final Release Review. NO
    partial packets or incremental review requests.

============================================================
END OF GATE 2 RESULT
============================================================