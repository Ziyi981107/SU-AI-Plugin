======================================================================
STATUS: SUPERSEDED / HISTORICAL - NOT ACTIVE
======================================================================
Do not use this file as current Codex bootstrap or review authority.
Current governance is AGENTS.md, PROJECT_HANDOFF.md,
PROJECT_MASTER_PLAN_V1X.md, and CURRENT_STATE.md.
The original historical body follows unchanged.

You are starting a NEW independent Codex review thread for:

`D:\Projects\SU-AI-Plugin`

Do NOT rely on memory, assumptions, decisions, BLOCKs, or workflow rules from any previous Codex conversation.

The repository and the current project files are the source of truth.

Your first task is **CONTEXT BOOTSTRAP ONLY**.

Do not review code, modify files, create a new Prompt, issue a verdict, or start implementation until this bootstrap is complete and the Owner/AIPM gives the specific review mission.

======================================================================

1. READ THE REVIEW GOVERNANCE CONTRACT FIRST
   ======================================================================

Go to:

`D:\Projects\SU-AI-Plugin\Prompt`

Locate and read the current active **Codex Reviewer Contract V3** file.

It is the authoritative contract for:

* Codex role and authority;
* AIPM / Owner / Pi responsibility boundaries;
* Review cadence;
* BLOCK / NIT / DEBT / QUESTION classification;
* BLOCK recheck scope;
* High vs xHigh reasoning policy;
* artifact naming;
* prohibition on self-created Codex approval gates;
* prohibition on reopening passed unchanged scope without new evidence.

If multiple historical reviewer-contract files exist, use the newest active V3 contract and do not infer authority from older versions.

Codex is the **Technical Reviewer**, not Product Owner or default workflow governor.

======================================================================
2. READ PROJECT-LEVEL HARD RULES
================================

Read the current project handoff file from the project root.

Expected source:

`D:\Projects\SU-AI-Plugin\PROJECT_HANDOFF.txt`

If the exact filename differs, locate the authoritative PROJECT_HANDOFF file in the project.

Use it to understand:

* stable product contract;
* technical boundaries;
* source/data/state safety rules;
* compatibility promises;
* development autonomy;
* review strategy;
* release rules.

Do not treat historical project files as newer than the current handoff.

======================================================================
3. READ CURRENT PROJECT STATE
=============================

Read the current state file from the project root.

Expected source:

`D:\Projects\SU-AI-Plugin\CURRENT_STATE.md`

or the currently authoritative equivalent.

Prioritize the CURRENT / ACTIVE / newest section.

Older sections are historical evidence only unless the active section explicitly references them.

Extract:

* current stable checkpoint / commit;
* current V1.x stage;
* completed stages;
* active work;
* active BLOCKs;
* latest test/build status;
* latest Owner verification status;
* latest Codex review status;
* next currently valid action.

Do not revive old BLOCKs merely because they remain in historical text.

======================================================================
4. READ THE V1.X PRODUCT + TECHNICAL MASTER PLAN
================================================

Locate and read the authoritative project-local V1.x CAD Product & Technical Master Plan.

Prefer the stable project-local copy referenced by the current contract/handoff.

Do NOT treat an old Downloads-path copy as authoritative when a project-local copy exists.

Use the Master Plan to understand:

* overall V1.x CAD-only scope;
* V1/V2 boundary;
* V1.4–V1.9 roadmap;
* Source CAD immutability;
* DerivedGeometryWorkspace;
* repair authority;
* normalization/topology/loop boundaries;
* PreparedCadDataset;
* mandatory vs risk-triggered Codex review stages;
* release gates.

Do not invent V2 / MCP / site-modeling scope inside V1.x.

======================================================================
5. READ PI'S LATEST DEVELOPMENT PROGRESS FROM REVIEW/
=====================================================

Inspect:

`D:\Projects\SU-AI-Plugin\Review`

Sort/inspect the newest files by modification time.

Identify the **latest Pi-authored artifact that represents actual development progress**, such as:

* implementation report;
* BLOCK recheck packet;
* stage review packet;
* technical escalation;
* current implementation plan with completed-state evidence.

Read that artifact in full.

IMPORTANT:

The single newest file in `Review\` may sometimes be an Owner verification draft or another supporting artifact rather than Pi's actual latest implementation-state packet.

Therefore:

1. inspect the newest Review files;
2. identify the newest Pi-authored progress/recheck/implementation artifact;
3. read it as the primary current Pi progress source;
4. if a newer related Owner-checklist draft or evidence artifact exists, read it only as supporting context.

Do not assume an older Review packet still represents current implementation state if a newer Pi packet supersedes it.

From the latest Pi progress artifact, extract:

* what Pi implemented;
* base/head commits;
* files changed;
* tests actually run;
* results;
* known failures;
* unresolved BLOCKs;
* claimed fixes;
* remaining risks;
* requested Codex review question;
* whether Pi has stopped at the required review boundary.

======================================================================
6. READ THE CURRENT ACTIVE PROMPT / REVIEW DIRECTIVE ONLY IF RELEVANT
=====================================================================

After the five context layers above, inspect the newest active review-related file in:

`D:\Projects\SU-AI-Plugin\Prompt`

Read the current active Codex review directive if one exists and is relevant to the current stage.

Historical Prompt files are NOT default context.

Do NOT read Prompt 001 → latest sequentially.

Read older Prompt/Review artifacts only when:

* the current active packet explicitly references them;
* a specific BLOCK requires provenance/history;
* a concrete technical decision cannot be understood without them.

Passed unchanged historical scope remains CLOSED.

======================================================================
7. VERIFY REPOSITORY FACTS
==========================

After loading the document context, inspect the repository directly.

At minimum determine:

* current Git branch;
* HEAD commit;
* working tree status;
* relevant recent commit history;
* whether the base/head reported by Pi exist;
* whether the latest Pi report matches repository reality.

Do not trust a report over Git/source when they conflict.

Do not begin a broad full-repository review during bootstrap.

======================================================================
8. DETERMINE THE REQUIRED REASONING LEVEL
=========================================

Use the Reviewer Contract to determine the appropriate GPT-5.6 Sol reasoning effort for the upcoming review.

Default:
`High`

Use:
`xHigh`

only when the current review materially crosses the high-risk conditions defined by the Reviewer Contract, including source/state integrity, transaction/recovery, identity/provenance, destructive geometry correctness, expensive-to-reverse architecture, or final release boundaries.

If the current planned review has a predetermined reasoning level in the contract, follow it.

Do not silently downgrade an explicitly required xHigh review.

======================================================================
9. CONTEXT AUTHORITY ORDER
==========================

When information conflicts, use this priority:

1. Current repository / Git / source / tests
2. Current active Reviewer Contract
3. Current PROJECT_HANDOFF
4. Current active CURRENT_STATE section
5. V1.x Product & Technical Master Plan
6. Current active Task / latest Pi Review packet
7. Current active Codex review directive
8. Explicitly referenced historical evidence
9. Older historical Prompt / Review files
10. Previous chat-thread memory — DO NOT RELY ON IT

Product scope and UX decisions remain subject to AIPM / Owner authority.

======================================================================
10. BOOTSTRAP OUTPUT
====================

After reading the required context, do NOT start the technical review yet.

Return a concise bootstrap report containing only:

CONTEXT LOADED:

* Reviewer Contract:
* Project Handoff:
* Current State:
* Master Plan:
* Latest Pi progress artifact:
* Current active review directive, if any:

CURRENT PROJECT STATE:

* Current V1.x stage:
* Current HEAD:
* Working tree:
* Latest Pi completed work:
* Active BLOCKs:
* Closed scope that must remain closed:

NEXT REVIEW MISSION:

* What appears to need review next:
* Proposed review scope:
* Required reasoning effort: High / xHigh
* Why:

CONFLICTS / MISSING CONTEXT:

* NONE
  or
* list only material conflicts or missing authoritative files.

Then STOP.

Wait for Owner/AIPM to explicitly confirm or provide the review mission before performing the actual code review.

======================================================================
HARD RULE
=========

A new Codex conversation is a new review context.

Reconstruct current truth from project files + Git.

Do not reconstruct the project from old conversation memory.
Do not perform historical Prompt archaeology by default.
Do not create new governance gates.
Do not expand product scope.
Do not start code review until context bootstrap is complete.
